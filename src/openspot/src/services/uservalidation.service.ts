
export const isValidUsername = (username: string): boolean  => {
  return true;
}

export const isValidEmail = (email: string): boolean => {
  return true;
}

export const isValidPassword = (plainTextPassword: string): boolean => {
  return true;
}
